{% render 'partials/head.liquid' %}
{% render 'partials/header.liquid' %}
<main class="portfolio">
    <section class="portfolio-intro">
        <h1>Portfolio</h1>
        <p>Here is a selection of projects I have worked on.</p>
    </section>

<section class="portfolio-items">
    {% for item in portfolioItems %}
        {% render 'partials/portfolio-item.liquid', project: item, customclass: "scroll-animated" %}
    {% endfor %}
</section>
</main>
