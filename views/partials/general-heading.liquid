<h{{ heading_level }} class="heading {{ my_class }}">
    {{ content }}
    {% if extra_element %}
        <{{ extra_element }}> 
            {{ extra_content }} 
        </{{ extra_element }}>
    {% endif %}
</h{{ heading_level }}>